.env
.env.local
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.DS_Store
.idea/
.vscode/
*.code-workspace
.streamlit/secrets.toml
_inspect*.py
_dropdowns.json
_dump.json
_inspect_out.txt
_drop.txt

# Excel workbooks — the the organisation Cameroon screener contains real RFP data
# and must not enter the repo. EXCEL_SOURCE_PATH in .env points each
# deployment at its own workbook location (OneDrive / SharePoint /
# whatever) so the path stays deployment-local.
*.xlsx
*.xlsm
*.xls

# Local debug / scratch outputs
*.log
.cache/
.pytest_cache/

# Playwright browser binaries (heavy, downloaded by `playwright install`)
.playwright/
playwright/.local-browsers/
